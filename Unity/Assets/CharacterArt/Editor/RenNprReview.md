# Ren NPR shading review

This isolated scene compares the current V2 prototype under its old diffuse shader and a new project-owned NPR shader. Both sides reference the exact same frozen FBX, shared mesh assets, source textures, normalization, face controls, and camera framing. It is a shading study, not a corrected jaw/hair candidate. The user's selected Tripo Studio H A-open cheeks/jaw source is separate and is not integrated here.

Scene: `Assets/CharacterArt/Generated/Preview/Scenes/RenNprReview.unity`. New assets are under `Assets/CharacterArt/Generated/RenNprReview/`. The existing `RenCompleteHead.unity`, V2 geometry/materials, complete-head captures, and standalone viewer remain unchanged. Original artist reference index 0 is visible by default.

`RenNprReviewController` composes the existing complete-head controls and applies the same mouth, independent blink, gaze, hair, and cap state to both panels. It checks shared-mesh identity during capture. The source's six `capOn` bindings remain at 100 while the cap is shown. Both lighting stages follow matching colored-point trajectories; the moving-light toggle freezes or resumes their phase.

The NPR face frame uses the displayed root's forward/right/up axes and a cached bound calculated from the frozen head vertices in candidate-local space for center and half-width. Material instances are cloned once per displayed candidate and reused for lighting/unlit switches. Frame vectors are set on those instances, without MaterialPropertyBlocks. This avoids MaterialPropertyBlock use; the headless ShaderUtil probe reports 'Not initialized', so actual SRP Batcher compatibility is not certified. Review orbit moves the cameras, not the model root. The center follows that local reference; half-width uses the length of the transformed local X extent, so rotating a world-space AABB cannot change it. Production bone attachment and head-turn validation remain separate work.

The builder requires a real `RenNprReviewManifest.json` binding the frozen FBX hash, shader asset/hash, and preset asset/hash. Baseline albedo maps, tint, and vertex-color encoding come from the frozen V2 manifest. Each preset must map an exact source material name and its shader floats/colors. Textured V2 slots retain white tint and their original texture assets. The new shader's unlit mode remains an albedo diagnostic. Source normal maps are not used.

The scene owns `RenNprReviewPipeline.asset`, cloned from the inherited URP asset with main shadows enabled at 2048 resolution, one cascade, distance 6 and Medium soft filtering. The new controller binds it in Play mode and restores the previous graphics/quality pipeline when destroyed. The standalone builder temporarily selects the same asset to retain the needed shader variants, then restores the scratch settings. The shared `GymPipeline.asset` is not edited. These are desktop preview settings; additional-light shadows are disabled and phone performance is not measured. Directional keys request soft shadows with depth/normal bias 0.025 and near plane 0.05. The old diffuse baseline has no shadow caster or shadow receiving; this difference is part of the comparison.

Only use the existing isolated scratch project at `C:/Users/jetha/AppData/Local/LucidLoopScratch/ren-eye-import-verification` for automated builds. The main Unity project may already be open; never start a second Editor on it. Coordinate a single scratch Editor owner with the shader agent. Entry points are `LucidLoop.CharacterArt.Editor.RenNprReviewBuilder.Build` and `BuildWindowsViewer`; set `-renNprPlayerOutput B:/lucid-loop/.local/ren-npr-review-v1-final/RenNprReview.exe`.

The player supports `-renNprCapture ABSOLUTE_DIRECTORY`. Each pose advances at least 12 actual player frames before an explicit URP GPU camera render. It writes separate matched old-diffuse/NPR images, actual controls, light phases, cap weights, and source/shader/preset/runtime hashes. `-renNprQuick` captures neutral and source-color front; `-renNprRevision` captures front/quarter in neutral and two club phases. The full suite adds front/quarter/profile, two deterministic moving-light phases, partial diagonal gaze with half blink, full blink, and open A. These are actual live-renderer captures, not baked pose substitutes or phone-performance measurements.

The completed soft-shadow player produced 28 actual GPU images with valid recorded hashes, at least 12 player updates per pose, matching shared meshes, and six correct cap weights throughout. Old/NPR unlit front PNGs are byte-identical. Neutral front/quarter/profile, both club phases, half blink plus diagonal gaze, full blink, and open A are included under `art/generated/characters/ren/parts-workflow-v1/npr-style-v1/unity-review/live/`. The NPR shader compiled for the exercised D3D11 paths with zero ShaderUtil messages and all four expected passes. The headless batching probe remains uninitialized, including after synchronous pass compilation; no batching, additional-shadow, Forward+, Metal, or phone-performance acceptance is claimed. Existing source art remains a prototype; no visible player was launched.
