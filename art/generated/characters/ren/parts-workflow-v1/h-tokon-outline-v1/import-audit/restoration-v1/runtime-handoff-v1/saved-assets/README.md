# Verified mouth meshes

These four files are exact copies of the assets used in the successful actual
Unity mouth review. `files.json` records the original Unity paths and hashes.
The working scene now carries these assets at the recorded paths in
`B:/lucid-loop/Unity`; follow the parent runtime handoff for required inverse-scale
children. The recorded external source project is historical.
These assets are unbound diagnostic meshes, not shared-rig or final-art approval.
Do not use the old eye meshes from the broader restoration experiment.
The parent review still depends on its documented scene, shader and texture inputs;
this folder alone is not a standalone Unity project.
