# Current H mouth runtime handoff

[runtime-handoff.json](runtime-handoff.json) is the exact allowlist and integration contract. It selects only four verified saved meshes: `Ren_H_Head`, `Ren_H_InnerLipWall`, `Ren_H_OralCavity` and `Ren_H_Tongue`. The latest viewer binding snapshot is preserved in [current-mouth-bindings.json](current-mouth-bindings.json). The viewer confirmed that these remain direct references to the unchanged `073e47cc…` FBX: head topology, UVs and cuts have not changed. New eyes, support and the temporal interface patch are separate objects.

[RenDesignerMouthImport.cs](RenDesignerMouthImport.cs) implements the bounded scratch Editor binding as `Add(RenDesignerEyeReview review, string handoffPath)`, returning the four renderers in the order above. Its public `AuditPath` property is populated after the call with a unique JSON path under `Assets/CharacterArt/Generated/RenDesignerMouthReview`. The helper performs no import, source save or scene save. The caller owns the new diagnostic scene and player build. Offline compilation against the actual current runtime assembly and Unity 6000.3.24f1 references passed. The actual Unity binding result is recorded below.

Before creating any children, it checks the frozen handoff and saved-mesh hashes, source FBX subasset identities, recorded source-node local transforms and default weights, exact ordered shape names/frame weights, saved position/normal/tangent arrays, and current material slot validity. Current selected material variants may differ from the earlier snapshot; their actual references and serialized values are preserved and recorded. Every corresponding ordered submesh triangle must match Basis positions within 2e-6 native units, UV components within 1e-6, and base normals/tangents within 2e-6. Cyclic rotations of a triangle are allowed; reversed winding, arbitrary face reordering and nearest-vertex remapping are rejected. A failed gate rolls back created children and the four renderer enabled states, and retains a failure audit.

Keep every original node and ancestor transform. Add a new bone-free skinned renderer on an identity child with **local scale 0.0001**, assign its exact verified mesh and preserve the donor's material slots and render settings. Disable only the original renderer. Do not multiply weights by 10000, put an uncompensated mesh directly on the old renderer, or replace the scene's actual registration with the frozen audit matrix. The latest viewer applies additional display transforms; the existing parent hierarchy must continue to supply them. Use the saved mesh's scaled bounds for the new renderer and preserve the existing face-frame lighting calibration.

For semantic `a=jawOpen_A` and `s=mouthSeal`, both in 0–1:

| Renderer | Actual Unity weights |
| --- | --- |
| Head and inner lip wall | `jawOpen_A = 100*a`; `mouthSeal = 100*s*(1-a)` |
| Cavity and tongue | `jawOpen_A = 100*a`; no seal key |

Default rest is `a=0,s=1`. Static teeth, new eyes, current support, cap, hair and all attachments remain in place. The current 62-triangle temporal patch has canonically zero A/seal deltas, as recorded by its import audit; do not give it historical eye controls. Rebind the review's head renderer reference to the new child while retaining `FaceFrame` and the current shader/material variants.

Preserve the existing open/closed lip maps and texture transforms. On the existing closed-endpoint material slot, use `t=clamp(a/0.75)` and `_ClosedWeight=1-t*t*(3-2*t)`. Other slots retain zero closed weight. The review clones materials during initialization and refinement components may swap variants, so apply only this scalar after initialization or through a per-slot property block. Do not overwrite shared material assets or replace the eye/support materials.

The one staged GPU test runs after the viewer's current forehead/iris/return priorities. Capture `a=0,0.25,0.5,0.75,1`, then return to `0`, all with `s=1`, from front, three-quarter and right profile: 18 captures with fixed lighting/camera/cap/hair. The interior samples exercise A and seal simultaneously. Record actual weights, closed-map weight, material/map identities, face frame, child/source transforms and frame IDs. Inspect oral continuity, lip paint, unchanged eye/patch boundaries and the return to closed rest. This is a new integration test; the completed broad CPU sweep need not be repeated.

The actual scratch Unity binding passed using helper SHA `3317e03c55177a68a644252e09f93845613ec42ec1ef764b466de7c0f32523f8`; the exact [binding audit](actual-binding-audit.json) and [independent result verification](actual-binding-verification.json) are retained here. All 646,488 rendered corners matched, including the head's original 119,741 welded vertices versus the saved mesh's 121,050 split vertices. UV, base-normal and base-tangent errors were exactly zero; maximum native Basis error was 9.095e-8, actual child compensation error 5.973e-8, and face-frame bounds error 1.193e-7. All 110 preexisting transforms and 52 unrelated renderer references/states passed preservation checks. The viewer's player reported capture completion; visual review is separate and is not asserted by these numerical checks.

The exact executed helper is preserved as [RenDesignerMouthImport-executed.cs.txt](RenDesignerMouthImport-executed.cs.txt). The current `.cs` revision (`98e5652d…`) additionally moves the success-audit write inside the guarded transaction, so a disk-write exception also rolls back the binding; failure logging is best-effort. This correction passed offline compilation and has not been attributed to the earlier successful Unity run. It does not alter mesh comparisons, compensation, materials or pose data.

These four saved meshes passed serialization and individual CPU poses. They contain **99 tiny zeroed canonical rows, all on the head and all below the 2e-6 native-unit tolerance**; the full 14-mesh diagnostic contained 161. No above-tolerance motion is zeroed. GPU mixture appearance still requires review, and the dense prototype is not a 40k-triangle production character or a canonical-female-rig binding. All eight historical saved eye/lash meshes and both saved hair meshes are explicitly excluded.
