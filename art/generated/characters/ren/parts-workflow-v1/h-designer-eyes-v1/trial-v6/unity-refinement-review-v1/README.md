# Actual Unity refinement comparison on neutral Ren v6

Forty-two actual D3D11 player captures compare six independently controlled states on the same frozen H073e head, neutral designer-eye v6, corrected support v2 and fitted cap/hair geometry. The baseline front is pixel-identical to the prior corrected support capture. The unchanged original artist sheet is included at equal source-pixel scale in every portrait pair; the calibrated source camera has its existing mouth/chin residual and was not warped to improve likeness.

States: `baseline`; `receiver-classification` uses R directly for received-shadow classification with original face strength .15; `receiver-005` additionally uses .05; `clean-hair` swaps only the exact paired pigment and derived shadow maps; `interface-patch` adds the verified local temporal patch; `combined` enables receiver .05, both clean hair maps and patch. Material/color/geometry changes remain separable. Actual per-frame shader/property/map names and toggle readbacks are in `RefinementLiveEvidence.json`. The two receiver-only states are pixel-identical to baseline in unlit mode.

The receiver candidate reduces the strongest mask band but does not remove all shutter/forehead/support discontinuities. Root selected its classification plus .05 attenuation as a provisional next lighting baseline. The clean hair map pair removes the periodic marbling, but its quiet, warm clumps still miss the artist's finer pale-blond strands; it is not final hair selection. The overall source-paired likeness remains unacceptable as a finished character.

The patch-only quarter changes exactly 601 pixels: 594 former clear-background pixels are filled and seven previously covered pixels change, matching the source occlusion audit. Its affected bounding box is x1042..1064/y614..672. Two crops retain actual screenshot pixels at nearest-neighbor 4x scale; the intended upper temporal notch visibly closes while lower gaps and fan fragments remain. The patch is a selected local repair only. The separate lower-iris slit affects280 pixels versus old support (273 exact clear-background pixels plus seven boundary pixels); its exact clear-background coverage persists in all six front states and is explicitly not hidden by the lighting change.

Inputs: head FBX073e, eye FBXee7f, support FBXa74d, patch FBX45ed; complete SHA256 values appear in the evidence. The candidate shader is a separately named copy with receiver include1c4a197d; original Tokon shader bytes remain unchanged. Hair maps are clean basec6b294 and paired shadowe18bc5, sampled once without another shadow multiplier. The patch passed actual Unity native-marker, drawn-corner XYZ/UV/linear-color and zero-endpoint checks; Unity splits its 64 authored vertices into151, retaining62 triangles. Its canonical A/seal endpoints are static. Imported missing tangents are irrelevant to this diffuse, non-normal-mapped material and are not claimed repaired.

Scratch scene: `Assets/CharacterArt/Generated/Preview/Scenes/RenDesignerRefinementReview.unity`. Local player: `.local/ren-designer-refinement-v1/RenDesignerRefinementReview.exe`. Build entry: `LucidLoop.CharacterArt.Editor.RenDesignerRefinementReviewBuilder.BuildWindowsViewer`, with `-renDesignerPatchSource <patch-export-v1>` and `-renDesignerPlayerOutput <new exe>`. Capture flag: `-renDesignerRefinementCapture <fresh folder>`. Independent receiver, hair and patch controls also exist in the internal viewer. This pass used a hidden actual player that exited after capture; no visible launch or main Unity scene promotion occurred.

Reproduction depends on the prior frozen neutral/Tokon/support review source assets in scratch plus the source packages cited above. This evidence package is not a standalone Unity project. Tested runtime/editor/helper and shader snapshots, import settings, exact source hashes and all captures are retained here. No canonical morph restoration, speech/blink/gaze animation, shared-female body binding, final style acceptance or iPhone15Plus performance result is claimed.
