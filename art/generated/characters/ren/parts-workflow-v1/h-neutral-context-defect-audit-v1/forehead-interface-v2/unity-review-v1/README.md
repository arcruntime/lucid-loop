# Graded forehead response: actual Unity pair

Sixteen actual Windows-player captures compare the selected 189-triangle material partition against the bounded graded adjacent-head R field in UV1.x. The new iris pigment, lower return, directional hair and receiver classification/.05 stay fixed in both states. Root selected the graded field as the next provisional baseline after viewing front/quarter. Ear and side-support boundaries remain unresolved; this is not whole-skin or style approval.

The baseline front is pixel-identical to the previous selected eye/hair combined frame. The unlit pair is pixel-identical. All16 hashes and actual visibility/property/camera readbacks pass. The field changes2384 front pixels,12 quarter pixels and0 profile pixels; front forehead flecks are reduced. No neck/back response change appears in these matched frames. See PixelVerification.json and ForeheadFieldLiveEvidence.json.

The helper verifies864 canonical/imported vertices,1478 triangles, source attribute and concatenated-index hashes unchanged, UV1 previously absent, and maximum correspondence error8.4622e-8 native units. It copies the existing support and writes only UV1.x, then merges the two submesh index groups in their existing concatenation order. The original renderer remains available unchanged as the baseline; field0 alone would not recreate that partition baseline. Candidate uses one material with FaceMode.9, fallbackR0, receiver.05 and field1.

Frozen shader1f8cb881... / HLSLd74c36c3... compile and render in the player. The shader differs from selected receiver1c4a... only by the interpolated UV1 R override. No source mesh, pigment, original UV0, eye silhouette, mouth controls or phone-performance claim changes.

Reproduce in the existing isolated scratch project by copying source snapshots to their corresponding Runtime/Editor paths, invoking LucidLoop.CharacterArt.Editor.RenDesignerForeheadFieldReviewBuilder.BuildWindowsViewer with -renDesignerForeheadFieldSource pointing to this folder's parent and -renDesignerPlayerOutput pointing to a fresh local executable. It opens RenDesignerEyeHairReview and saves a separate RenDesignerForeheadFieldReview scene. Capture that player with -batchmode -renDesignerForeheadFieldCapture <fresh-directory> -logFile <fresh-log>. Exact local output and saved-scene hash are in BuildProvenance.json. No visible player launch or main Unity promotion occurred.
