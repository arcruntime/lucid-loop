# Actual Unity support and static jaw bridge review

The corrected source export v2 restores the scalp support to its reviewed identity world placement. These eighteen actual D3D11 player captures compare old support against the separately imported support and jaw bridge on frozen H073e head plus neutral designer-eye v6. Head/eye/hair/cap geometry, material shader presets and lighting are fixed. The baseline front is pixel-identical to the earlier neutral v6 front.

Unity marker, rendered color and actual neutral geometry checks pass. All 36 bridge boundary anchors lie within 8.33e-8 native units of the current baked historical head. Bridge A/seal targets are identical to Basis in the source; this is static neutral closure, not working mouth deformation. Explicit half-byte Unity color quantization allowance is recorded. No canonical morph restoration was applied.

Visual review: scalp now stays under the cap and profile jaw-gap coverage improves. Support/face shading boundaries and rough extraction edges remain. A thin clear-background slit below the image-right iris is newly exposed: the same 280 pixels change in neutral, club and unlit, so this is coverage rather than received shadow. This package is a provisional review baseline, not likeness, final style, rig, animation or phone-performance acceptance.

`superseded-v1` preserves the source-placement failure. Its marker/array checks passed because they faithfully reproduced a misregistered standalone support; actual complete-head captures caught the scalp rising through the cap. Export v2 changes only support world placement; bridge and global markers are unchanged. The first import helper also failed on an empty optional JSON payload and was corrected without changing data/tolerances.

Scratch scene: `Assets/CharacterArt/Generated/Preview/Scenes/RenDesignerSupportV2Review.unity`. Local player: `.local/ren-designer-support-v2/RenDesignerSupportReview.exe`. Builder entry: `LucidLoop.CharacterArt.Editor.RenDesignerSupportV2ReviewBuilder.BuildWindowsViewer`, with `-renDesignerSupportSource <support-export-v2>` and `-renDesignerPlayerOutput <new exe>`. Capture flag: `-renDesignerSupportCapture <fresh output>`. The main Unity project was untouched; player closed after capture. Inputs and dependencies remain in the frozen neutral/Tokon/support packages; tested helper and runtime snapshots are included.
