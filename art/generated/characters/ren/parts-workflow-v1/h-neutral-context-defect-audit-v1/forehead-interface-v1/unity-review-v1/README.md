# Local forehead response: actual Unity causal pair

Fourteen actual D3D11 captures compare a bounded189-triangle support region against its original response. Both states keep receiver-classification/.05 facial attenuation, clean hair maps and temporal patch fixed. A private support mesh copy partitions1478 triangles into1289 baseline and189 forehead triangles. Positions, normals, UVs, colors, triangle orientation and imported source mesh are unchanged. This adds one material draw, without new geometry.

Only the selected region changes FaceMode0/R0/face shadow.15 to FaceMode.9/R1/face shadow.05. Adjacent sampled head-control R is at least.996; lower-confidence transition regions, neck and back remain unchanged. Baseline front equals the prior combined capture exactly; the unlit pair is identical. Actual camera/material gates pass. Front changes cover1698 pixels within x594..1034/y489..610. Target forehead flecks reduce; excluded edge patches remain visible. Root selected this local response provisionally, not final shading or likeness.

The first capture was invalid: an unused old material remained in the base viewer list after partitioning, causing Awake to fail and retain a stale camera rectangle. Its log and representative image remain in failed-attempt. Corrected code filters actual renderer material membership and asserts full camera rectangle, projection, pose and unlit state before accepting images. No failed image is counted as a valid material comparison.

Scratch scene: Assets/CharacterArt/Generated/Preview/Scenes/RenDesignerForeheadReview.unity. Corrected player: .local/ren-designer-forehead-v1-retry2/RenDesignerForeheadReview.exe. Build entry: LucidLoop.CharacterArt.Editor.RenDesignerForeheadReviewBuilder.BuildWindowsViewer, with -renDesignerForeheadPartition <forehead-material-partition.json> and -renDesignerPlayerOutput <new exe>. Capture flag: -renDesignerForeheadCapture <fresh output>. Main Unity was untouched; players exited. No source mesh changes, new eye controls, production rig or phone-performance claim. This evidence depends on prior frozen refinement/support/Tokon assets; it is not a standalone Unity project.
