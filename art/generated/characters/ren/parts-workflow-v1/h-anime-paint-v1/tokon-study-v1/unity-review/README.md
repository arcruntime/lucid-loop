# Tokon shader — provisional controlled Unity review

Root selected the refined face controls as the next provisional shader baseline after reviewing the actual front, quarter, profile, club and head-turn captures. The eye silhouette/geometry remains rejected; there is no final style, complete character or phone-performance approval. No generated H/Tokon scene or art asset was promoted into main Unity.

The player rendered the exact frozen H FBX073e geometry in three modes: previous PaintedAnimeNPR, Tokon with original facial controls, and Tokon with revised controls. Both shader families use the same original open-base texture9750 in this comparison. The original rose PNG is not silently compared against a different base. The only Tokon face revision is spatial R coverage through the chin and face strength.45→.90 on mapped head/lids; palettes, shader bytes, GBA, geometry, cap response and all mouth/eye/head controls remain fixed.

All three1536×1536 unlit RGB images are pixel-identical (independently checked with NumPy). Actual selected material assignment, unlit value, animated face frame, control-map identity/strength, morph weights and matching pose/camera/light state passed assertions. Ten poses produced30 primary screenshots; four separate closed-paint poses bring the raw total to34. This folder retains22 representative PNGs; the full record lists additional raw files under `.local/ren-tokon-review-v2/live-review-retry/`.

The revised mask removes the jagged lower-lip/chin shade while retaining neck/ear bands. Eye-region rings/source defects and the cap's broad block highlight remain visible. The separate closed-lip paint diagnostic is explicitly labeled and uses the reviewed jaw envelope; it is not folded into the primary shader comparison. Oral material semantic role is OralPreserved across the two Tokon mask modes, with source tints and double-sided Cull0 preserved; the initial conversion reused the Eye fallback preset. See the exact distinction in review-evidence.json.

Both new shaders compiled in Unity6000.3.24f1 without reported errors. The player actually used Direct3D11, a study-owned soft-main-shadow pipeline and PerPixel additional lights. Additional lights are bounded, unshadowed first-N accents. Ink compiled, but no outline shell was rendered. SRP Batcher API returned code1, Not initialized (); compatibility is unverified. No iPhone/Metal or sustained30fps result is implied.

The first V2 process failed before rendering when the unchanged video transport exceeded its15-second initial-frame deadline. The exact executable was retried once and passed without source changes. failed-initial-attempt/ preserves the failure and log hashes. The successful material capture waits for a decoded reference frame then uses fixed manual poses; it is not a new full video playback/scrubbing test, and reference audio is disabled.

Exact tested runtime/editor/shader bytes are under tested-source/. They were built only in the isolated scratch Unity project. RenTokonManifest.json maps exact original sources and map hashes; FaceControlsV2.json records the mask derivative. Local player: `.local/ren-tokon-review-v2/RenTokonReview.exe`. It was run hidden for capture and exited; no visible viewer was launched. For capture, use `-renTokonCapture <fresh-folder>`; build entry is `LucidLoop.CharacterArt.Editor.RenTokonReviewBuilder.BuildWindowsViewer` with `-renTokonPlayerOutput <new-exe>`, restricted to the authorized scratch project.

The earlier `.local/ren-tokon-review-v1/` remains superseded diagnostic history because its previous-shader unlit state was stale. It is not used as the matching-color proof in this folder.
